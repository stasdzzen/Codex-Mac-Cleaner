# Badge

- Источник: официальный `base-mira`.
- Композиция: Slot заменён на `useRender` Base UI; внешний API использует `render` вместо `asChild`.
- В текущих потребителях `asChild` не использовался, поэтому поведение не изменилось.
- Проверка: typecheck после замены.
