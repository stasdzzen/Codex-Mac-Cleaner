# Tabs

- Источник: официальный `base-mira`.
- Примитивы: `Root`, `List`, `Tab`, `Panel` из Base UI.
- Клавиатурная навигация и выбранная вкладка сохранены; Base UI использует ручную активацию вкладок по умолчанию.
- Проверка: typecheck после замены.
